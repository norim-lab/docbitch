# Frontend Environment Variables

# API URL - only needed if frontend and backend are on different domains
# For Railway deployment with separate services, set this to your backend URL
# Example: VITE_API_URL=https://your-backend.up.railway.app/api

# Leave empty for local development (uses Vite proxy)
VITE_API_URL=
